<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="description" content="<%= post.SEOkeyWords %>"/>
    <link rel="stylesheet" href="css/post.css">
    <title><%= post.title %></title>
    <link rel="icon" href="images/book-open-solid.svg">
    <link href="https://fonts.googleapis.com/css2?family=Gotu&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/a1e74affc8.js" crossorigin="anonymous"></script>
</head>
<body>
    <div class="photo-container">
        <img src="<%= post.coverImage %>">
    </div>
    <div class="blog-heading"><h1 class="blog-heading-title"><%= post.title %></h1></div>
    <div class="content-container wrapper">
  
        <div class="blog-links"> 
            <span>Share: </span>
            <span><i class="sm-btn fab fa-twitter"></i></span>
            <span><i class="sm-btn fab fa-linkedin-in"></i></span>
        </div>
        <div class="blog-details filter-container">
            <p> Post by <%= post.author %> on <%= post.createdDate.toLocaleDateString() %></p>
            <p> Tags:
            <% tags.forEach((tag)=>{ %>
                 <a href="/filter?category=<%= tag.trim() %>"><span class="filter <%= tag %>-filter" id="<%= tag %>-filter">#<%= tag.trim() %></span></a>
           <%  }) %>
             </p>
        </div>
        <div class="blog-content">
           <a href="/posts"><p class="btn back-home"> <i class="fas fa-chevron-left"></i>Back Home </p></a> 
            <h3 class="blog-excerpt">
                <%= post.excerpt %>
            </h3>
            
         
            <% firstP.forEach((p)=>{ %>
                <p class="blog-full-content"> <%= p %></p>
           <% }) %>   
           <div class="in-blog-image">
            <img  src="<%= post.blogImage1 %>">
            <p class="img-description"> <%= post.img1Description %> </p>
        </div>
       
        <% secondP.forEach((p)=>{ %>
            <p class="blog-full-content"> <%= p %></p>
       <% }) %>
       <div class="in-blog-image">
        <img  src="<%= post.blogImage2 %>">
        <p class="img-description">  <%= post.img2Description %> </p>
    </div>
    <% thirdP.forEach((p)=>{ %>
        <p class="blog-full-content"> <%= p %></p>
   <% }) %>
   <% if (post.blogImage3) { %>
    <div class="in-blog-image">
        <img  src="<%= post.blogImage3 %>">
        <p class="img-description">  <%= post.img3Description %> </p>
    </div>
           
  <% } %>
  <% lastP.forEach((p)=>{ %>
    <p class="blog-full-content"> <%= p %></p>
<% }) %>
      </div>
        </div>
    </div>
    <div class="page-container wrapper">
        <!-- <span class="btn"> <a href="/">Next:  <i class="fas fa-angle-right"></i> </a> </span> -->

            <div class="recommended-post-header">
                <h3> Recommended Posts</h3>
            </div>
            
            <div class="recommended-post-container">
                <% recommendedPost.forEach((post)=>{ %>
                    <a href="/<%= post.slug %>"><div class="recommended-post-card">
                        <div class="recommended-post-photo">
                            <img src="<%= post.coverImage %>">
                        </div>
                        <div class="recommended-post-title">
                            <h3> <%= post.title %></h3>
                        </div>
                    </div></a>
                <% }) %>

                </div>

    </div>

    <%- include('footer') %>
<script src="js/index.js"></script>
</body>
</html>